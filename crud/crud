1.) first create a new folder name : crud
2.) creat an file app.js
3.) after complete this run some basic commands
 a) npm init -y
 b) npm intall express
 c) npm install nodemon
 d) npm install ejs
 e) npm install mongoose
 4.) after excute these commands
 5.) add basic code inside the app.js file
        const express = require('express')
const { default: connectDB } = require('./db/connectdb')
        const app = express()
        const port = 3000

        app.get('/', (req, res) => {
        res.send('Hello World!')
        })

        app.listen(port, () => {
        console.log(`Example app listening on port http://localhost:${port}`)
        })
 6.) open package.json file and add 
     "type":"module"

7.)create new folder inside the main folder name : db
   inside the db folder create connectdb.js